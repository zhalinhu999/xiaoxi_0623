<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.edu.swufe.appframe">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <!--<uses-permission android:name="android.permission.GET_ACCOUNTS" />-->
    <!--<uses-permission android:name="android.permission.READ_PROFILE" />-->
    <!--<uses-permission android:name="android.permission.READ_CONTACTS" />-->
    <!--<uses-permission android:name="android.permission.VIBRATE" />-->

     <!--获取GSM（2g）、WCDMA（联通3g）等网络状态的信息 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- 获取wifi网络状态的信息 -->
    <!-- <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" /> -->
    <!-- &lt;!&ndash; 保持CPU 运转，屏幕和键盘灯有可能是关闭的,用于文件上传和下载 &ndash;&gt; -->
    <!-- <uses-permission android:name="android.permission.WAKE_LOCK" /> -->
    <!-- &lt;!&ndash; 获取sd卡写的权限，用于文件上传和下载 &ndash;&gt; -->
    <!-- <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" /> -->
    <!-- &lt;!&ndash; 允许读取手机状态 用于创建BmobInstallation &ndash;&gt; -->
    <!-- <uses-permission android:name="android.permission.READ_PHONE_STATE" /> -->

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/app_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/app_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ScrollingActivity"
            android:label="@string/title_activity_scrolling"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MyActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity android:name=".HuodongFrams" />
        <activity android:name=".XueyListActivity" />
        <activity android:name=".XueyActivity" />
        <activity android:name=".XuexActivity" />
        <activity android:name=".ShetuanActivity" />
        <activity android:name=".BmobDemoActivity" />
        <activity android:name=".RegisActivity" />
        <activity android:name=".Func_First" />
        <activity android:name=".LostfindActivity" />
        <activity android:name=".CycleActivity" />
        <activity android:name=".BookActivity" />
        <activity android:name=".CalendarActivity" />
        <activity android:name=".UpdatepwdActivity" />
        <activity android:name=".UploadActivity"></activity>
    </application>

</manifest>